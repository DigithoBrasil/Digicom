extends ../layout

block content
	h1 Lançamento de custos

	form(method="post", action="/lancamento/salvar")
		p
			label(for="data") Data:
			input(id="data", type="date", name="lancamento[data]")

		p
			label(for="finalidade") Finalidade: 
			input(id="finalidade", name="lancamento[finalidade]")

		p
			label(for="detalhesDaCompra") Detalhes da compra: 
			input(id="detalhesDaCompra", name="lancamento[detalhesDaCompra]")

		p
			label(for="valor") Valor: 
			input(id="valor", type="number", min="0.01", step="0.01", max="10000", name="lancamento[valor]")

		input(type="submit", value="Salvar")